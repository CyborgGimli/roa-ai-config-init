# AI Agents

Orchestration definitions for plugin-level agents. Each plugin defines how an AI agent should approach ROA tasks in that domain.

See individual plugin AGENTS.md files for specific agent definitions.
