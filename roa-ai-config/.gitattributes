* text=auto eol=lf
*.mjs text eol=lf
*.json text eol=lf
*.md text eol=lf
*.yaml text eol=lf
*.yml text eol=lf

# Generated build output: collapse in diffs and exclude from language stats.
# The marketplace manifest is written to the repository root, so it is covered
# by the root .gitattributes instead.
dist/plugins/** linguist-generated=true
