# Hooks

Git hooks and Maven hooks for ROA projects.

- Pre-commit: Code quality checks
- Post-merge: Update dependencies
- Pre-push: Verify tests pass

Configure via `.git/hooks/` or `.maven/` in target project.
