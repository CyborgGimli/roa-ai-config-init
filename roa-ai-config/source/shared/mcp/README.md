# MCP Servers

Model Context Protocol server configurations for AI agent use.

- GitHub (code context, PR metadata)
- DevTools (file operations, project structure)
- Swagger (API documentation parsing)

Each plugin defines which MCPs to load.
